<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.ten_characters.researchAndroid.activities.AboutActivity"
    android:orientation="vertical"
    style="@style/AppTheme.Activity">

    <!-- Web Link -->
    <TextView
        android:id="@+id/about_weblink"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/website_text"
        android:textColor="@color/accent_dark_indigo"
        android:layout_centerHorizontal="true"
        android:autoLink="web"/>

    <TextView
        android:id="@+id/about_version"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_version"
        android:textColor="@color/accent_dark_indigo"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        />

    <!-- Main Links -->
    <ListView
        android:id="@+id/about_options_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/about_weblink"
        android:layout_above="@+id/about_version" />

</RelativeLayout>
